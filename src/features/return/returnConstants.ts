/** Demo-only constants — not final commerce rules. */

export const RETURN_WINDOW_DAYS = 30;

/** Mock return-shipping deduction shown in refund preview (USD cents). */
export const DEMO_RETURN_SHIPPING_FEE_CENTS = 799;
