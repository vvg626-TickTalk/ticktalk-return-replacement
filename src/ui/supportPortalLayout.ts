/**
 * @deprecated Import from `@/ui/supportTheme` directly.
 * Barrel retained so existing paths keep working during refactors.
 */
export {
  supportBody,
  supportBodySmall,
  supportErrorCallout,
  supportPanel,
  supportSectionHead,
  supportTextarea,
  supportToolbarBtn,
} from './supportTheme';
