/**
 * Form controls — aliases for `supportTheme` PPT field styles (16px text, iOS zoom-safe).
 */
export { supportField as fieldControl, supportFieldMono as fieldControlMono, supportTextarea as fieldTextarea } from './supportTheme';
